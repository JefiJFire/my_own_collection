---
- name: Use my_own_role from collection
  hosts: localhost
  collections:
    - my_own_namespace.yandex_cloud_elk
  roles:
    - role: my_own_namespace.yandex_cloud_elk.my_own_role
      vars:
        file_path: /tmp/from_collection.txt
        file_content: "Created by collection role!"
